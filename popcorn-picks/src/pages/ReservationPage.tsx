import styled from "styled-components";
import Reservation from "../styles/components/Reservation";

const ReservationPage = () => {
  return (
    <>
      <Reservation></Reservation>
    </>
  );
};

export default ReservationPage;
